# Node & Laravel
node_modules
vendor
/public/hot
/public/storage
/storage/*.key
/storage/pail
# .env*   ← コメントアウトまたは削除
.env      # ← .env は無視するけど
!.env.production  # ← .env.production は無視しない

.phpunit.result.cache
Homestead.*
npm-debug.log
yarn-error.log

# SQLite DB
# /database/*.sqlite
!database/database.sqlite




# IDE & System
.idea
.vscode
.DS_Store
Thumbs.db
.fleet
.zed

# Vite build (only allow built files)
!public/build/
!public/build/**

# Allow critical public files
!public/.htaccess
